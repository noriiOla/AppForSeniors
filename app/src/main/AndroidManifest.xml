<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.olastandard.appforseniors">
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ManuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExampleActivity" />
        <activity android:name=".Contacts.ContactTypeActivity" />
        <activity android:name=".Contacts.ContactListActivity"></activity>
        <activity android:name=".Contacts.AddContactActivity"></activity>
        <activity android:name=".Contacts.EditContactActivity"></activity>
        <activity android:name=".smsActivitys.MessagerActivity" />
        <activity android:name=".smsActivitys.MessagerListActivity" />
        <activity android:name=".smsActivitys.NewSmsActivity"></activity>

    </application>

</manifest>